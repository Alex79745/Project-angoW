{
  "name": "hotel-frontend",
  "version": "0.1.0",
  "scripts": {
    "build": "mkdir -p dist && cp -r index.html dist/",
    "test": "jest"
  },
  "dependencies": {},
  "devDependencies": {
    "jest": "^29.0.0"
  }
}
